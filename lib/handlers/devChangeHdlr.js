module.exports = function (netcore, eps, data) {
    // listen shepherd event: 'devChange'
    netcore.commitDevReporting(permAddr, devAttrs);
    netcore.commitGadReporting(permAddr, auxId, gadAttrs);
};